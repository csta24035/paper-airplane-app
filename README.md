
## 起動方法　https://csta24035.github.io/paper-airplane-app/
## 紹介動画 https://youtu.be/mHP6NbPjvm0
# paper-airplane-app
## 要件定義
### 目的
#### 個人で様々な​紙飛行機の​折り方を​個々で​記録・​保存し、​飛距離と​セットで​閲覧できるWebアプリ


### 利用者の入出力
#### 入力  
「紙飛行機の名前」(必須)文字入力数1～100文字  
「完成した紙飛行機の画像」(任意)1枚当たり10MBまでの容量制限  
「折り方（テキスト）」(任意)入力範囲0～5000文字  
「折り方（画像）」(任意)1枚当たり10MBまでの容量制限  
「折る回数」(任意)入力範囲は整数で0回～999回  
「飛距離」(任意)入力範囲0.1m～9999.9m  
「作成日」(任意)手入力で都度更新可  
「備考メモ」(任意)改行可
#### 出力  
名前  
完成した紙飛行機の画像（先頭画像のみ、画像無しならプレースホルダー表示）    
飛距離  
折る回数  
作成日  
折り方  
備考メモ
上記を紙飛行機ごとにまとめてセットで閲覧できるように表示 


### 制約  
Webアプリのみ  
データは端末内のブラウザストレージに保存  
実装方式はIndexedDBを利用  
データはサーバーへ送信しない  
利用可能な端末はスマートフォン・タブレット・PC  
紙飛行機の名前は重複可  
飛距離はm（メートル）単位で小数点入力も可  
折る回数は手入力  
作成日は任意の日付を指定  
備考メモの文字数制限は256文字まで  
完成画像は3枚まで登録可能  
折り方画像は50枚まで登録可能  
折り方画像と折り方テキストは両方必須ではない  
データ件数は1つの端末に最大100件  
IndexedDB容量超過時には保存失敗時はエラーメッセージ表示  
ブラウザデータ削除時はデータも消失し復旧機能は提供しない  
最大件数到達時は100件で、最大まで登録済みの場合には新規登録を禁止してエラーメッセージ表示  
画像枚数超過時は完成画像4枚目以降は選択不可  
折り方画像51枚目以降は登録不可 


### 受け入れ基準
紙飛行機の登録が可能  
一覧表示が可能  
データが再起動後も残る  
画像付きでの閲覧が可能  
折り方を手順ごとに管理（手順1：説明テキスト・画像　手順2：説明テキスト・画像）  
一覧画面では名前・完成画像 ・飛距離・折る回数・作成日を表示。  
一覧から詳細画面へ遷移する形式  
詳細画面の表示順は名前・完成画像・飛距離・折る回数・作成日・折り方・備考メモ  
ソートは昇順・降順の両方必要  
飛距離順・折る回数順・作成日順・名前順でソート可能  
検索機能（名前検索）  
登録後の紙飛行機情報を編集可能  
削除機能  
削除時に確認ダイアログを表示  
アップロード可能な画像形式はJPGとPNG  
画像サイズに上限が無いため画像の自動リサイズや圧縮（長辺1920px以内・JPEG品質80%・PNGはリサイズのみ）が必要  
対応ブラウザはSafari・Chrome・Edge・Brave  
同時利用者は1人  
必須項目のみで登録可能  
最大件数100件まで登録可能  
登録後一覧に反映  
全項目を編集可能  
編集内容は永続  
昇順・降順を切り替え可能  
同値の場合は登録順を維持  
部分一致検索  
大文字小文字を区別しない  
0件時にメッセージ表示  
ブラウザ再起動後もデータが保持される。端末再起動後も保持される。 


### 非目標
ユーザー登録  
AIによる折り方提案  
クラウド同期  
複数ユーザー管理  
他端末との同期は対象外  
QR共有は対象外

## 4種の設計図
### ユースケース図風
![alt text](image-6.png)
#### 図のポイント解説  
アクター: システムを利用する「利用者（User）」を定義し、主要なユースケースに実線（---）で繋いでいます。同時利用者は1人という要件に基づき、アクターは1種類のみです。  

<< include >> (包含関係)：登録（AirplaneForm）や編集時には、要件にある「画像の自動リサイズ・圧縮（ImageUploader）」や「手順の管理（InstructionEditor）」が必須プロセスとして組み込まれるため、include で繋いでいます。  
削除実行時の「確認ダイアログ表示」も要件として必須のため include としています。
全てのデータ操作（登録、表示、編集、削除）は、IndexedDB（indexedDb）を介して行われるため、データ永続化処理を包含しています。  

<< extend >> (拡張関係)：「100件到達時」や「IndexedDB容量超過時」という特定の条件を満たした場合のみエラーメッセージの表示が行われるため、登録処理からの extend（拡張）として表現しています。  

### クラス図
![alt text](image-5.png)
#### クラス図のポイント  
データモデルの分離：要件で規定されているデータ（名前、完成画像最大3枚、飛距離、折る回数、作成日、備考メモ）を Airplane クラスに定義し、0〜50個作成される手順を Instruction クラスに切り出しました。これらは コンポジション（黒いひし形 *--）で結びつけています。  
多重度の明記：Airplane と Instruction の間には "1" *-- "0..50"（1つの紙飛行機に対して折り方は最大50件）を定義。  
indexedDb と Airplane の間には "1" o-- "0..100"（DB内に保存できる紙飛行機データは最大100件）を定義し、集約（白いひし形 o--）を用いています。  

UIとロジックの依存関係：ListPage や AirplaneForm 等のUIコンポーネントが、それぞれ子コンポーネント（SearchSort や ImageUploader など）を内包する形で表現しています。画像自動リサイズと圧縮の要件は ImageUploader の操作として定義しました。  

### シーケンス図
紙飛行機の登録フロー
![alt text](image-7.png)
  
  
紙飛行機の削除フロー
![alt text](image-8.png)
  
#### 図のポイント解説  
4層アーキテクチャの分離：画面(UI): AirplaneForm や DetailPage など、ユーザー入力を受け付け、表示を担当。  

コントローラ：上限件数の判定や、ImageUploader に相当する画像処理ロジックなど、ビジネスルールを担当。  

モデル(DB)：indexedDb を介した実際のデータの読み書き、永続化を担当。  

loop（繰り返し）：登録フロー（図1のNo.5〜6）にて、ユーザーが添付した画像の枚数分だけ、画像最適化処理（長辺1920px以内、JPEG80%等）を繰り返す構造にしています。  

alt（条件分岐）：登録フローでは「登録件数が100件に達しているかどうか」による全体分岐と、その内部での「IndexedDBの容量超過により保存に失敗したか」のネストされた分岐を実装しています。  
削除フローでは「確認ダイアログでユーザーがOK/キャンセルのどちらを選択したか」による処理の分岐を表現しています。  

### 状態遷移図
![alt text](image-9.png)
#### 図のポイント解説  
初期状態 ([ ]) と終了状態 ([ ])：初期状態から新規作成を開始することで、データの入力状態（Drafting）へ遷移します。  
削除確認ダイアログで「OK」を選択し、indexedDb からデータが完全に削除された時点が終了状態（ライフサイクルの終わり）となります。  

Drafting（入力・編集状態）：AirplaneForm 上での状態です。基本情報の入力（EnteringData）を中心に、画像の添付（ImageUploader）や手順の追加（InstructionEditor）を行き来する内部状態を持たせています。  

Validating（検証・保存処理状態）：ユーザーが保存を試みた際の一時的な状態です。要件にある「最大100件の上限チェック」「画像の圧縮・リサイズ」「DBへの保存」の順に処理が進みます。  

エラー（件数上限や容量超過）が発生した場合は、元の入力状態（Drafting）に差し戻され、エラーメッセージが表示される遷移（トリガー）を表現しています。  

Saved（保存済み状態）：indexedDb に永続化された状態です。AirplaneList での一覧表示や、DetailPage での詳細表示の間の遷移を行います。  
SearchSort による検索・ソートは、一覧表示状態（DisplayedInList）における自己遷移として表現しています。
ここから「編集」をトリガーとして Drafting に戻るか、「削除」をトリガーとして ConfirmingDelete へ進む分岐が発生します。

## テストケース表
| # | テスト対象 | テスト観点(正常/境界/異常) | テスト条件 | テスト手順(1行) | 期待値(1行) | 結果(○/ ) |
|---|---|---|---|---|---|---|
| 1 | 飛行機登録(新規) | 正常 | 全必須・任意項目に正当な値を入力して保存 | 新規作成画面で名前、飛距離、折り数、作成日、メモを入力し「保存」を押す | データが正常に保存され、一覧画面のリストに新しい飛行機が追加表示されること |〇|
| 2 | 飛行機登録(編集) | 正常 | 既存データの一部を変更して保存 | 一覧から既存アイテムの「編集」を押し、名前の末尾に「改」を追記して「保存」を押す | データが上書き保存され、一覧および詳細画面で変更後の名前が表示されること |〇|
| 3 | 飛行機リスト(削除) | 正常 | 既存の飛行機データを削除 | 一覧画面で任意のアイテムの「削除」を押し、表示された確認ダイアログで「OK」を押す | 対象のアイテムがリストから消滅し、再度ページを読み込んでも表示されないこと |〇|
| 4 | 飛行機詳細表示 | 正常 | リストから特定の飛行機の詳細画面へ遷移 | 一覧画面に表示されている任意の飛行機項目（名前など）をクリックする | 詳細ページへ遷移し、その飛行機の飛距離や折り数、メモなどの全情報が表示されること |〇|
| 5 | 検索機能 | 正常 | 存在する名前のキーワードで検索 | 検索ボックスに、一覧に存在する飛行機名の一部（例：「紙」）を入力する | 入力したキーワードを名前に含む飛行機のみがリストにフィルタリング表示されること |〇|
| 6 | ソート機能 | 正常 | 飛距離を基準に昇順・降順を切り替え | ソートドロップダウンで「飛距離」を選択し、ソート順（昇順/降順）の切り替えボタンを押す | リスト内のアイテムが飛距離の数値に従って正しく並び替わること |〇|
| 7 | 画像アップロード(完成図) | 正常 | 有効な画像ファイル(JPEG/PNG)を選択 | 完成図のファイル選択ボタンを押し、PC内の有効なJPEG画像を選択する | 選択した画像がプレビューエリアにサムネイル表示され、保存対象として認識されること |〇|
| 8 | 手順(Instruction)追加 | 正常 | 折り方の手順ステップを新規追加 | 「手順の追加」ボタンを押し、表示されたテキストエリアに折り方の説明を入力する | 入力したテキストが新しいステップとしてUI上に追加され、保存可能な状態になること |〇|
| 9 | 手順(Instruction)削除 | 正常 | 追加した手順ステップを削除 | 追加済み手順ステップの横にある「削除」ボタンを押す | 該当の手順ステップが画面から消去され、残りのステップの番号が自動で詰められること |〇|
| 10 | 検索機能(該当なし) | 正常 | 存在しないキーワードで検索 | 検索ボックスに、登録データに絶対含まれない文字列を入力する | リストが空になり、「該当データがありません」等のメッセージが画面に表示されること |〇|
| 11 | 飛行機登録(名前の境界) | 境界 | 名前を最小文字数（1文字）で入力 | 新規作成画面で名前に「あ」の1文字だけを入力し、他の必須項目を埋めて「保存」を押す | エラーにならず保存され、名前「あ」の飛行機が一覧に正常に表示されること |〇|
| 12 | 飛行機登録(名前の境界) | 境界 | 名前をシステム想定の最大文字数で入力 | 名前に50文字の文字列を入力し、他の必須項目を埋めて「保存」を押す | 入力した長文が途切れたりレイアウトを崩したりせず、正常に保存・表示されること |〇|
| 13 | 飛行機登録(飛距離の境界) | 境界 | 飛距離に0を入力 | 飛距離入力欄に「0」を入力し、他の必須項目を埋めて「保存」を押す | エラーにならず保存され、飛距離「0」として正常に表示されること |〇|
| 14 | 飛行機登録(折り数の境界) | 境界 | 折り数に最低値の「1」を入力 | 折り数入力欄に「1」を入力し、他の必須項目を埋めて「保存」を押す | エラーにならず保存され、折り数「1」として正常に表示されること |〇|
| 15 | 飛行機登録(任意項目の境界) | 境界 | 任意項目をすべて未入力のまま保存 | 名前・飛距離・折り数・作成日のみを入力し、メモ・画像・手順を空にして「保存」を押す | 正常に保存され、詳細画面では未入力項目が空欄または「登録なし」として扱われること |〇|
| 16 | 飛行機リスト(0件表示) | 境界 | 登録データが全くない状態で一覧を表示 | 登録データを全て削除した状態（または初期状態）で一覧ページを開く | リスト領域が崩れず、「登録された飛行機はありません」等の空状態を示すUIが表示されること |〇|
| 17 | 検索機能(空白の境界) | 境界 | 検索キーワードにスペースのみを入力 | 検索ボックスに全角スペースを1つだけ入力してフィルタリングを実行させる | クラッシュせず、スペースを含むデータのみ表示されるか全件表示のまま安全に維持されること |〇|
| 18 | 手順追加(最大件数の境界) | 境界 | 手順を多数（例：20件）追加 | 「手順の追加」ボタンを20回連続で押し、全ての入力欄にテキストを入力して保存を押す | 動作が極端に重くならず20件分の入力枠が正常に生成・保存され、詳細画面で全て表示されること |〇|
| 19 | 画像アップロード(サイズ境界) | 境界 | 許容最大ファイルサイズ上限ギリギリの画像を選択 | 完成図の画像選択で、システム上限（例：5MB）ちょうどのファイルサイズの画像を選択する | エラーにならず、プレビュー表示およびアップロード・保存処理が正常に完了すること |〇|
| 20 | 作成日入力(締切/当日の境界) | 境界 | 作成日カレンダーで操作当日の日付を選択 | 新規作成画面の作成日入力欄で、カレンダーから「今日」の日付を選択して保存を押す | 正常に受け付けられ、一覧および詳細画面の作成日に当日の日付が正しく反映されること |〇|
| 21 | 飛行機登録(必須チェック) | 異常 | 必須項目(名前)を未入力で保存を試みる | 新規作成画面で名前を空欄のままにし、飛距離や折り数だけ入力して「保存」を押す | 保存処理が実行されず、名前入力欄付近に「必須項目です」等のエラーメッセージが表示されること |〇|
| 22 | 飛行機登録(不正な数値) | 異常 | 飛距離にマイナスの数値を入力 | 飛距離の入力欄にキーボードで「-10」と入力し、「保存」を押す | 保存処理が実行されず、「0以上の数値を入力してください」等のバリデーションエラーが表示されること |〇|
| 23 | 画像アップロード(非対応形式) | 異常 | 画像以外の非対応ファイルを選択 | 画像選択ダイアログで、画像ではないファイル（例：.pdfや.txt）を選択する | アプリがクラッシュせず、「対応していないファイル形式です」等のエラー通知が安全に表示されること |〇|
| 24 | DBアクセス(保存障害) | 異常 | ストレージ機能が制限された状態で保存を試みる | ブラウザの開発者ツールでIndexedDBの書き込み権限をブロックした状態で「保存」を押す | アプリが白画面でクラッシュせず、「保存に失敗しました」等の安全なエラーメッセージが表示されること |〇|